# === ALIASES === 
alias less="less -RM"
alias vim=nvim
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'
alias python="/usr/bin/python3"

# === FUNCTIONS === 

# rg with a pager
rgl() {
  rg -p --stats "$@" | less -XF
}
